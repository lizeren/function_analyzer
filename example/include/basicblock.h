#ifndef BASICBLOCK_H
#define BASICBLOCK_H

#include <vector>

// Function declaration to find the maximum of three integers.
int maxOfThree(int a, int b, int c);

// Function declaration to find the index of the first negative number in a vector.
int findFirstNegative(const std::vector<int>& vec);

// Function declaration to count the number of prime numbers up to a given integer.
int countPrimes(int n);

#endif 
