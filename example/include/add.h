#ifndef ADD_H
#define ADD_H

// Declares the function 'add' that sums two integers.
int add(int a, int b);

// Declares the function 'printHello' that prints "Hello, world!".
void printHello();

#endif 
